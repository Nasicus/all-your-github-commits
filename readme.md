# All Your GitHub Commits

A simple react app (with vite) which allows you to search for all commits of a specific author across all repos in a GitHub organization.

The app calls the GitHub API directly therefore there is no API / server part needed.
